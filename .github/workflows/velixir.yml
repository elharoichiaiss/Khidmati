name: Deploy to velixir

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: velixir/velixir
        with:
          api-key: ${{ secrets.VELIXIR_API_KEY }}
